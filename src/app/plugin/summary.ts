import * as moment from 'moment';
import { Plugin } from '../model/plugin';

export const summaryQueryPlugin: Plugin = {
  tag: 'plugin/summary',
  name: '🔰️💭️ Summary Query Action',
  config: {
    type: 'tool',
    default: false,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    icons: [{ label: $localize`🔰️💭️`}],
    filters: [
      { query: 'plugin/summary', label: $localize`🔰️💭️ summarize query`, group: $localize`Plugins 🧰️` },
    ],
    actions: [
      { tag: 'plugin/summary', labelOff: $localize`summarize`, global: true }
    ],
    description: $localize`Create tabular data.`,
  },
};

export const summaryPlugin: Plugin = {
  tag: '+plugin/summary',
  name: '🔰️ Summary',
  config: {
    type: 'tool',
    default: false,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    icons: [{ label: $localize`🔰️`}],
    filters: [
      { query: '+plugin/summary', label: $localize`🔰️ summary`, group: $localize`Plugins 🧰️` },
    ],
  },
  generateMetadata: true,
};
