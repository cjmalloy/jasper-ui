import * as moment from 'moment';
import { Plugin } from '../model/plugin';

export const rootPlugin: Plugin = {
  tag: 'plugin',
  name: $localize`/`,
  config: {
    default: true,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    description: $localize`This plugin stores system wide config for
     customizing the locked icon, the modlist, and banned urls`,
    theme: null,
    icons: [
      { label: $localize`🔒️`, tag: 'locked', title: $localize`Locked` },
      { label: $localize`✔️`, tag: '_moderated', title: $localize`Moderated` },
    ],
    actions: [
      { tag: '_moderated', labelOff: $localize`approve` }
    ],
    filters: [
      { query: '!_moderated', label: $localize`🛡️ modlist`, group: $localize`Mod Tools` },
    ],
    bannedUrls: [
      '//bit.ly/',
      '//ow.ly/',
      '//tinyurl.com/',
      '//is.gd/',
      '//buff.ly/',
      '//adf.ly/',
      '//bit.do/',
      '//mcaf.ee/',
      '//su.pr/',
    ]
  },
};

