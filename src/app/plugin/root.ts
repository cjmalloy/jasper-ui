import * as moment from 'moment';
import { Plugin } from '../model/plugin';

export const rootPlugin: Plugin = {
  tag: 'plugin',
  name: $localize`Root`,
  config: {
    default: true,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    description: $localize`This plugin stores system wide config.`,
    icons: [ { label: $localize`🔒️`, tag: 'locked', title: $localize`Locked` } ],
    actions: [
      { tag: '_moderated', labelOff: $localize`approve`, labelOn: $localize`✔️` }
    ],
    filters: [
      { query: '!_moderated', label: $localize`modlist`, group: $localize`Mod Tools` },
    ],
    bannedUrls: [
      '//bit.ly/',
      '//ow.ly/',
      '//tinyurl.com/',
      '//is.gd/',
      '//buff.ly/',
      '//adf.ly/',
      '//bit.do/',
      '//mcaf.ee/',
      '//su.pr/',
    ]
  },
};

