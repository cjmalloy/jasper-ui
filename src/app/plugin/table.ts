import * as moment from 'moment';
import { Plugin } from '../model/plugin';

export const tablePlugin: Plugin = {
  tag: 'plugin/table',
  name: '📏️ Table',
  config: {
    type: 'viewer',
    default: true,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    description: $localize`Activates built-in table support and allows users to create tables.`,
    submitInternal: $localize`📏️ table`,
    icons: [{ label: $localize`📏️`}],
    filters: [
      { query: 'plugin/table', label: $localize`📏️ table`, group: $localize`Plugins 🧰️` },
    ],
  },
};

export const spreadsheetPlugin: Plugin = {
  tag: 'plugin/sheet',
  name: '📏️ Sheet',
  config: {
    type: 'editor',
    experimental: true,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    description: $localize`Enables spreadsheet editor for plugin/table Refs.`,
    editor: $localize`📏️ sheet`,
    edits: ['plugin/table'],
  },
};
