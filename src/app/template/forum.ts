import * as moment from 'moment';
import { Template } from '../model/template';

export const forumTemplate: Template = {
  tag: 'forum',
  name: $localize`🎈️ Forum`,
  config: {
    experimental: true,
    generated: 'Generated by jenkins-ui ' + moment().toISOString(),
    submit: $localize`🎈️ forum`,
    view: $localize`🎈️`,
    description: $localize`Activates built-in Forum mode for viewing Refs.`,
    filters: [
      { query: 'forum', label: $localize`🎈️ forum`, group: $localize`Templates 🎨️` },
    ],
  },
  schema: {
    optionalProperties: {
      categories: { elements: { type: 'string' } },
    },
  },
};
