import * as moment from 'moment';
import { Plugin } from '../model/plugin';

export const modPlugin: Plugin = {
  tag: '+plugin/mod',
  name: '📦️ Mod',
  config: {
    type: 'plugin',
    default: true,
    generated: $localize`Generated by jasper-ui ${moment().toISOString()}`,
    internal: true,
    icons: [{ label: $localize`📦️`}],
    description: $localize`A mod which can be installed on your system.`,
    hasDefaults: true,
    hasClearCache: true,
    // TODO: JSON form
  },
  schema: {
    optionalProperties: {
      plugins: {
        elements: {
          properties: {
            tag: { type: 'string' },
            name: { type: 'string' },
          },
          optionalProperties: {
            config: { },
            generateMetadata: { type: 'boolean'},
            userUrl: { type: 'boolean'},
          },
        },
      },
      templates: {
        elements: {
          properties: {
            tag: { type: 'string' },
            name: { type: 'string' },
          },
          optionalProperties: {
            config: { },
          },
        },
      }
    }
  }
};
