import { DateTime } from 'luxon';
import { Mod } from '../model/tag';

export const saveMod: Mod = {
  plugin: [{
    tag: 'plugin/save',
    name: $localize`⭕ Save to list`,
    config: {
      generated: $localize`Generated by jasper-ui ${DateTime.now().toISO()}`,
      description: $localize`Mark Refs as saved for the current user`,
      icons: [{ response: 'plugin/save', label: $localize`⭕`, global: true, order: -1 }],
      filters: [
        { user: 'plugin/save', label: $localize`⭕ saved`, title: $localize`My saved Refs`, group: $localize`Filters 🕵️️` },
      ],
      actions: [{ response: 'plugin/save', labelOff: $localize`save`, global: true }],
      advancedActions: [{ response: 'plugin/save', labelOn: $localize`unsave`, global: true }],
    },
    generateMetadata: true,
    userUrl: true,
  }],
};
