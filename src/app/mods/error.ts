import * as moment from 'moment';
import { Plugin } from '../model/plugin';
import { Mod } from '../model/tag';

const errorPlugin: Plugin = {
  tag: '+plugin/error',
  name: $localize`⚠️ Error`,
  config: {
    mod: $localize`⚠️ Error`,
    default: true,
    generated: 'Generated by jasper-ui ' + moment().toISOString(),
    icons: [{ label: $localize`⚠️`, order: 3 }],
    filters: [
      { query: '+plugin/error', label: $localize`⚠️ error`, group: $localize`Plugins 🧰️` },
    ],
    actions: [
      { tag: '+plugin/error', labelOn: $localize`retry` },
    ],
    description: $localize`Marks a Ref as errored.`,
  },
};

const logPlugin: Plugin = {
  tag: '+plugin/log',
  name: $localize`🪵 Log`,
  config: {
    mod: $localize`⚠️ Error`,
    default: true,
    generated: 'Generated by jasper-ui ' + moment().toISOString(),
    settings: $localize`errors`,
    icons: [{ label: $localize`🪵`, order: 3 }],
    filters: [
      { query: '+plugin/log', label: $localize`🪵 logs`, group: $localize`Plugins 🧰️` },
    ],
    description: $localize`Logs associated with a Ref.`,
  },
  generateMetadata: true,
};

export const errorMod: Mod = {
  plugins: {
    error: errorPlugin,
    log: logPlugin,
  },
}
