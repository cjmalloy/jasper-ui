import { TemplateUiDirective } from './template-ui.directive';

describe('TemplateUiDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateUiDirective();
    expect(directive).toBeTruthy();
  });
});
