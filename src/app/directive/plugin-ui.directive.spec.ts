import { PluginUiDirective } from './plugin-ui.directive';

describe('PluginUiDirective', () => {
  it('should create an instance', () => {
    const directive = new PluginUiDirective();
    expect(directive).toBeTruthy();
  });
});
