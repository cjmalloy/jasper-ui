import { ActionLabelDirective } from './action-label.directive';

describe('ActionLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionLabelDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
