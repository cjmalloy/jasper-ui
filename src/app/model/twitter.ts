
export interface TweetData {
  url: string,
  author_name: string,
  author_url: string,
  html: string,
  width: number | null,
  height: number | null,
  type: string,
  cache_age: string,
  provider_name: string,
  provider_url: string,
  version: string
}
